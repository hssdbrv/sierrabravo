{
  "name": "sierrabravo",
  "version": "1.0.0",
  "description": "A Telegram bot hosted on Cloudflare Workers.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish",
    "build": "echo \"No build step (Workers module)\"",
    "lint": "eslint . || true",
    "format": "prettier --write . || true",
    "test": "echo \"No tests configured\" && exit 0"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.0.0",
    "eslint": "^8.50.0",
    "prettier": "^2.8.8"
  }
}